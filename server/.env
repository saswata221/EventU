PORT=5000
DB_USER=postgres
DB_PASSWORD=postgre123
DB_HOST=localhost
DB_NAME=MoviesDB
DB_PORT=5432


# server/.env
PORT=5000

# JWT & cookies
JWT_ACCESS_SECRET=replace_with_long_random_string
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
COOKIE_NAME=rtkn

# CORS (comma-separated during dev)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Email (dev: we just console.log links; fill when ready)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
MAIL_FROM=EventU <no-reply@eventu.local>

# For verify/reset links shown in console
APP_URL=http://localhost:5173
API_URL=http://localhost:5000
